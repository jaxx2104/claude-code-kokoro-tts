# claude-code-kokoro-tts

Claude Code の応答を Kokoro TTS で自動音声再生するhookシステム。
`install.sh` が `hooks/` のスクリプトを `~/.claude/hooks/` にコピーし、`~/.claude/settings.json` にhookを登録する。

hook編集時の技術コンテキストは `hooks/CLAUDE.md` を参照。
